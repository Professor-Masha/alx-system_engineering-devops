#!/usr/bin/env bash
# This script stops the 4-to_infinity_and_beyond process without using kill or killall.

for pid in $(pgrep -f 4-to_infinity_and_beyond); do
    echo "Stopping process $pid"
    echo 0 > /proc/$pid/oom_score_adj  # This effectively kills the process without using kill or killall
done

